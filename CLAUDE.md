# P17264 打表收尾：把 36/39 推到 38/39

你接手的是一个**已经做到 96% 的打表任务的最后一步**。请先完整读完本文件，再读 `doc/constraints.md`。不要跳过「已排除方向」一节——上一轮已经用约 15 种手段撞过墙，重走会浪费全部算力。

## 一、问题与口径

洛谷 P17264（ICPC 2017 乌鲁木齐站 F 题），题面见 `doc/problem.md`。星际争霸经济模拟：给定 $m<2^{64}$，求最早在何时矿物量达到 $m$。多测最多 52 组。

标准做法是**打表**：预计算 $F[0..830]$，查询时二分。

**折半口径（全程遵守）**：矿物量恒为偶数（初始 50，采矿 +8，成本 100/400/50），故一律折半存储以免溢出。

- $F[t]$ = 时刻 $t$（$=10t$ 秒）可达的最大**折半**矿物量
- 答案 $=10\cdot\min\{t : 2F[t]\ge m\}$

单步转移（折半后）：每步 10 秒；工人采矿 $+4$（1 步）；建补给站 $-50$、3 步后 $+8$ 补给；建基地 $-200$、12 步后 $+1$ 基地 $+10$ 补给；空闲基地造工人 $-25$ 矿 $-1$ 补给、2 步后出 1 工人且基地归位。初始 $x=25,s=6,w=4,b=1$。

## 二、目标

当前表 `data/table_best.txt`（831 项）对 39 个官方样例是 **36 对 3 错**。

**主目标**：把这两点提上去 → 38/39

| $t$ | 当前 $2F[t]$ | 需要 $\ge$ | 缺口（相对） |
|---|---|---|---|
| 333 | 10691283922 | 10691286350 | $2.27\times10^{-7}$ |
| 500 | 59770518667750 | 59770531908338 | $2.22\times10^{-7}$ |

**次目标**（成功率低，有余力再做）：$t=171$，需 $2F[171]\ge2476413$，当前 2476366，缺口 $1.90\times10^{-5}$。

**禁区（必须同时守住）**：$2F[173]<2745943$、$2F[331]<9641625025$、$2F[496]<48610229060556$、$2F[502]<66277611238091$。这四点的余量同样只有 $2.1\sim2.3\times10^{-7}$。

> **绝对不要试图整体抬高曲线。** 把所有约束折算到同一归一化尺度后，$t\approx500$ 处的可行窗口是**负的**（$-1.4\times10^{-9}$）：均匀抬高 $2.2\times10^{-7}$ 会修好 $t=500$ 但同时顶破 $t=496$，净收益为零。改进必须**逐点**。详见 `doc/constraints.md`。

**逐点提升为何合法**：表中每项都是「该时刻可达值」的一个下界，恒 $\le$ 真值；逐点取最大仍是下界，只会让答案单调趋近正确。前提是新值必须由一条**经 u64 精确重放验证过可行**的动作序列产生。

## 三、核心思路（本轮的新武器）

上一轮的两个可证明结论（推导见 `doc/solution.md` 第 5、5b 节）：

1. 严格 von Neumann 上界 $\rho^\ast=1.053027864$，与样例反推的真实增长率吻合到 7 位有效数字。
2. 精确前馈补给规则 $8p(t)=ab[1]-9\,ab[3]$（纯状态函数），实现上用等价的自校正形式
   $$8p\ \ge\ 2\,ab_1+ab_2+ab_3+2b-s-(as_1+as_2+as_3)$$
   连续与整数实现均精确达到 $\rho^\ast$。归一化曲线因此从持续下滑变为 $t\ge250$ 完全水平。

**本轮要用的新事实**：$t\approx466$ 处各量已达 $10^{10}\sim10^{13}$，**LP 松弛的整数性缺口约 $10^{-10}$，比需要的 $2.2\times10^{-7}$ 紧三个数量级——这一段 LP 就是精确解**。而实测兑现段的固定策略族距 LP 上界还有 $\approx5\times10^{-6}$ 的余量，是需求的 20 倍。

同一分析也解释了 $t=171$ 为何难：那里量级只有 $10^{6}$，取整损失本身就是 $\sim10^{-5}$，与目标缺口同量级。

## 四、仓库内容

```
doc/problem.md      题面
doc/solution.md     完整推导 + 已排除方向表（很长，按需查阅）
doc/constraints.md  39 样例双侧约束 + 归一化阈值   <- 必读
src/gen_table.cpp   打表生成器（束搜索 + 交接 + 三段兑现），产出当前最优表的那一版
src/lp.cpp          任意初始状态的精确 LP（3H 变量/4H 约束，z=0 天然可行故无需 Phase-1）
                    含 LPROUND 取整模拟与坐标下降修复
src/exact.cpp       开局精确全枚举 + 支配剪枝（自校验用）
src/dumpst.cpp      纯策略轨迹 + 滑动兑现（自校验用）
src/allcash.cpp     对一批状态施加步长 1 的三段兑现网格
src/finalcash.cpp   沿成长轨迹在 [T-CW,T] 每步做细网格兑现
src/check.py        39 样例校验 + 禁区余量 + 表不变量   <- 每次改表后必跑
src/merge.py        逐点取最大合并
data/table_best.txt 当前 36/39 的表
data/beam*.bin      束状态转储，t=105/115/125/135/170/180/190/200
```

`data/beam*.bin` 格式：`int32 状态数, int32 时刻 t`，随后是紧密排列的 `St` 结构体
（`u64 x,s,w,b; u64 aw[13],am[13],as_[13],ab[13];`，共 448 字节）。

> 注：`beam125/135` 来自 TMAX=180 的运行，$t>116$ 后 rollout 视野被截断，与完整运行的束略有差异。它们仍是**合法可达状态**，只是来源略有不同。`beam105/115/170/180/190/200` 未被截断。

## 五、流水线

### 阶段 0：自校验（必做）

直接跑 `bash run.sh selftest`。环境差异会静默产生假结果，三项全过才继续：

1. **表在小 $t$ 处是真实最优**：`src/exact.cpp` 全枚举 + 支配剪枝到 $t=25$，$t=1\ldots25$ 每一项都必须等于 `data/table_best.txt`。（已在本机确认，且用更大内存跑到 $t=30$ 也逐项相符：$2F[30]=1920$。）
2. **LP 必须是有效上界**：$T=10/20$ 时 LP 恰好取到整数最优 384/1014；$T=30$ 时 LP 给 1968 而真值是 1920——**这不是错误，而是 2.5% 的整数性缺口**。
3. **齐次缩放不变性**：LP 对初始状态一次齐次（约束右端 `X0c/S0c/B0c/G0f` 与目标全部线性于状态）。同一状态缩放 $10^6$ 前后求解，LP 值之比应等于缩放因子。

> 第 2 项是本方案核心判断的**直接实证**：$T=30$（各量个位到百位）时 LP 缺口高达 $2.5\times10^{-2}$，而 $t\approx466$（各量 $10^{10}\sim10^{13}$）时缺口降到 $\sim10^{-10}$。所以「小尺度下 LP 不可信、大尺度下 LP 即精确解」不是推测，是可以在阶段 0 亲眼看到的。同理也说明：**不要把 LP 用在 $t=171$ 这种小尺度上当作可达值**。

### 阶段 1：从束转储重建交接轨迹

对每个 `data/beam*.bin` 中的状态，按成长策略推进（即 `gen_table.cpp` 的 `act(..., pol=3, ...)`，其补给量用上面的自校正规则），记录 $t\in[280,340]$ 与 $t\in[440,510]$ 的状态。

**验证门**：用三段兑现网格（`src/allcash.cpp` 或 `finalcash.cpp`）从这些状态复算 $F[333]$、$F[500]$，必须能**复现或超过** `table_best.txt` 的对应值。若明显偏低，说明转储覆盖的种子时刻不足 → 转阶段 1b。

> 上一轮踩过的坑：按「总工人数」给状态排序来挑种子会选到一批退化状态（束里混有大量高矿物、低成长的兑现态），结果比表低 5.5%。请改用 LP 值或长时域成长值排序。

### 阶段 1b：仅在验证门失败时执行

用 `src/gen_table.cpp` 重跑完整生成器并**区间转储**交接轨迹状态。现有代码只有单时刻转储（交接段的 `if(t==TDUMP) HD.push_back(c);`，配合 `TDUMP` 参数写出 `hand.bin`），需改成 `if(t>=TDA&&t<=TDB)`。

运行参数用产出当前最优表的那一组：

```
830 3000 100 12000 8000 300 21 200 220 4 98 64 115 22 23 21 20 98 64 7
```

代价约 1～2 小时、峰值数 GB 内存。**若沙箱扛不住就跳过，如实说明**——不要为了绕过它而降低配置，那会产出与基线不可比的结果。

### 阶段 2：逐状态解 LP

对阶段 1 的每个状态解到目标 $T$ 的 LP（窗口 40～80 步）。求解前把状态整体缩放到 $\sim10^6$ 量级，解完再缩回，规避 long double 在 $10^{13}$ 量级的条件数问题。

### 阶段 3：带进位取整 + 修复

`src/lp.cpp` 现有取整是逐步 `floor`/`round`，有系统性损失（实测 $T=171$ 时取整只得 2474046，比固定策略族还低）。

**必须改为带进位取整**：对 $k,p,q$ 各维保留小数残差，累积到 1 时补一个整数单位。总损失从「每步 0.5」降到「全程约 1 单位」——在 $q\sim10^8$ 尺度上即 $\sim5\times10^{-9}$，远小于 $2.2\times10^{-7}$ 的预算。这一步是成败关键。

随后接 `lp.cpp` 已实现的坐标下降修复（含矿物中性交换动作 $k{-}8d,q{+}d$、$p{-}4d,q{+}d$、$k{-}2d,p{+}d$ 等——矿物通常已花光，只有等价代换才可行）。

最后用 u64 精确 `step()` 全程重放，逐步断言四条约束：$p+q\le w$、$k\le b$、$k\le s$、$25k+50p+200q\le x$。**未经这一步验证的值不得写入表。**

### 阶段 4：并回并全量校验

仅当新值**严格大于**表中该点时写入；重做前缀最大保持单调；跑 `python3 src/check.py <新表>`。

该脚本会打印 bad 数、三个目标点的缺口、以及四个禁区的余量。**任何禁区余量变成非正即判定该表不可用**，必须回退。

### 阶段 5：有余力再攻 $t=171$

同流程，目标 $T=171$、窗口起点 $t\in[120,175]$。按第三节的尺度分析成功率低，不要为它牺牲主目标。

## 六、已排除方向（不要重走）

上一轮对每一个可能来源都做了独立测试，**以下全部给出逐位相同的结果**，并把整个会话产生的约 200 张表逐点合并后缺口一字不变：

- 束宽 1×／2×／3×；开局单独加宽（4× 以上候选数组内存溢出）
- 用**精确 Pareto 前沿**替代开局（$t=24$ 处全枚举得 24074 个非受支配状态，束只留约 5000）
- 兑现网格步长 4／2／**1**；各段上限加宽；兑现时域 48／56／60
- 交接种子数 1～64；多准则选种；TKEEP 200／220
- 束搜索长度 TBEAM 182／220／400
- 长时域评估步数 NLONG 300／600
- 兑现参数 GNC 在 70～300 扫描（最优仍是 98）、KAC、TSPLIT
- 候选生成器对 $k,q$ 在上界附近加密（3 个 → 24 个）
- 每步在束内保留随机状态做多样化（多种子）
- 面向绝对目标时刻的额外束准则
- 交接种子选择的一处实现缺陷修正（原 `longVal` 只对矿物最少的一批候选计算）

终局侧另测了 7 种独立方法，**全部不如生成器**：

| 方法 | $2F[171]$ |
|---|---|
| 生成器（基线） | 2476366 |
| LP 对偶价格线性打分束搜索 | 2320942 |
| 以 LP 值为准则的两级筛选束搜索 | 2475614 |
| 从 LP 取整解出发的坐标下降 | 2474086 |
| **策略改进**（矿物中性单步偏离 + 尾部重最优化） | 无改进 |
| 末段线性价值大束搜索 | 2465890 |
| $t=135$ 全部 5779 状态步长 1 全网格 | 2469806 |

其中「策略改进」这条最有说服力：当前解在「单步偏离 + 最优续接」意义下是**不动点**，任何局部方法都出不去。

## 七、汇报要求

结束时如实说明：

- 最终 bad 数，以及三个目标点各自的最终值与缺口
- 四个禁区的余量（证明没有顶破）
- 阶段 0 三项自校验的实际输出
- 阶段 1 验证门是否通过；若走了 1b，说明实际配置与耗时
- 哪些新值经过了 u64 精确重放验证
- 若未能改进，明确说清 LP 上界与实际可达值的差距落在哪里——这本身是有价值的结论（可据此判定终局已达整数最优、缺口全在开局）

**不要伪造或外推未实际运行的结果。** 跑不动的步骤如实说跑不动。
