# P17264 打表收尾包

洛谷 P17264 [ICPC 2017 Urumqi R] Gathering 的打表任务，已做到 **36/39**，本仓库用于把剩余三个样例中的两个（$t=333$、$t=500$）攻下来。

给 Claude Code 会话的完整任务书在 **`CLAUDE.md`**（会话启动时自动加载）。

## 快速开始

```bash
bash run.sh selftest     # 阶段 0：工具链自校验，必须先过
bash run.sh check        # 看当前表的 39 样例结果与禁区余量
bash run.sh build        # 只编译，不跑
```

## 现状

| $t$ | 当前 $2F[t]$ | 需要 | 缺口 |
|---|---|---|---|
| 171 | 2476366 | $\ge$ 2476413 | $1.90\times10^{-5}$ |
| 333 | 10691283922 | $\ge$ 10691286350 | $2.27\times10^{-7}$ |
| 500 | 59770518667750 | $\ge$ 59770531908338 | $2.22\times10^{-7}$ |

主目标是后两个（同源）。$t=171$ 难度高一个数量级，列为次目标。

**注意还有四个禁区**（$t=173/331/496/502$）不得顶破，余量同样只有 $2.2\times10^{-7}$——所以不能整体抬高曲线，必须逐点改进。详见 `doc/constraints.md`。

## 环境要求

- `g++`，C++17，建议 `-O2`
- `python3`（仅标准库）
- 内存：主流程 < 1 GB；可选的完整生成器重跑（阶段 1b）峰值数 GB

## 目录

```
CLAUDE.md            任务书（冷启动必读）
doc/problem.md       题面
doc/solution.md      完整推导与已排除方向
doc/constraints.md   39 样例双侧约束
src/                 生成器、LP 求解器、校验脚本
data/table_best.txt  当前 36/39 的表（831 项）
data/beam*.bin       束状态转储
run.sh               流水线入口
```

## 已确立的结论（不要推翻重来）

- 严格 von Neumann 上界 $\rho^\ast=1.053027864$，与样例反推的真实增长率吻合到 7 位有效数字
- 精确前馈补给规则 $8p=ab_1-9\,ab_3$ 在连续与整数实现下均精确达到该上界
- 归一化曲线 $2F[t]/\rho^{\ast t}$ 在 $t\ge250$ 后完全水平（360.20973），即渐近速率已到顶，剩余缺口是开局常数

推导见 `doc/solution.md` 第 5、5b 节。
